---
phase: 01-monorepo-scaffolding
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/eslint.config.mjs
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/src/
  - apps/web/public/
  - pnpm-lock.yaml
autonomous: false
requirements:
  - MONO-01
  - MONO-03
  - MONO-04

must_haves:
  truths:
    - "pnpm --filter web dev starts the Next.js app on localhost:3000 without errors"
    - "pnpm --filter web typecheck reports 0 TypeScript errors"
    - "apps/web/ contains all app source files (src/, public/, config files)"
    - "apps/web/tsconfig.json extends ../../tsconfig.base.json"
    - "apps/web/package.json has name 'web' and all original app dependencies"
    - "Repo root contains no src/, public/, next.config.ts, eslint.config.mjs, postcss.config.mjs"
  artifacts:
    - path: "apps/web/src/app/page.tsx"
      provides: "Next.js home page (moved)"
      contains: "AppShell"
    - path: "apps/web/tsconfig.json"
      provides: "App TypeScript config"
      contains: "../../tsconfig.base.json"
    - path: "apps/web/package.json"
      provides: "App package manifest"
      contains: "\"name\": \"web\""
    - path: "apps/web/next.config.ts"
      provides: "Next.js config (moved)"
      contains: "NextConfig"
  key_links:
    - from: "apps/web/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "../../tsconfig.base.json"
    - from: "apps/web/src/**/*.ts"
      to: "apps/web/src/*"
      via: "@/* path alias in apps/web/tsconfig.json"
      pattern: "\"@/\\*\": \\[\"./src/\\*\"\\]"
---

<objective>
Move all Next.js app files from the repo root into `apps/web/`, create the app-specific `tsconfig.json` that extends the base config, and verify the app works correctly from its new location. This is the final and most impactful step of Phase 1.

Purpose: Complete the monorepo restructure. After this plan, the phase goal is met: `pnpm --filter web dev` runs the app identically to before.
Output: The complete app under `apps/web/`; repo root no longer contains app files.
</objective>

<execution_context>
@/Users/ivanmaierg/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivanmaierg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-monorepo-scaffolding/01-CONTEXT.md
@.planning/phases/01-monorepo-scaffolding/01-RESEARCH.md
@.planning/phases/01-monorepo-scaffolding/01-01-SUMMARY.md
@.planning/phases/01-monorepo-scaffolding/01-02-SUMMARY.md

<interfaces>
<!-- Files to move from repo root to apps/web/ (complete list): -->
MOVE THESE:
  src/           → apps/web/src/
  public/        → apps/web/public/
  next.config.ts → apps/web/next.config.ts
  eslint.config.mjs → apps/web/eslint.config.mjs
  postcss.config.mjs → apps/web/postcss.config.mjs
  components.json → apps/web/components.json
  next-env.d.ts  → apps/web/next-env.d.ts  (auto-generated, copy or let Next.js regenerate)

DO NOT MOVE (stay at root):
  .nvmrc          (Node version — shared by all packages)
  CLAUDE.md       (agent instructions — repo-level)
  AGENTS.md       (agent instructions — repo-level)
  pnpm-workspace.yaml
  turbo.json
  tsconfig.base.json
  package.json
  .npmrc
  .gitignore
  packages/
  .planning/
  README.md
  pdr.md

<!-- Current package.json content (to become apps/web/package.json): -->
Original name: "asciify" → rename to "web"
All dependencies and devDependencies move to apps/web/package.json
Add scripts: typecheck: "tsc --noEmit", clean: "rm -rf .next"
typescript devDep is already present

<!-- apps/web/tsconfig.json target shape (per D-07, Pattern 2): -->
extends: "../../tsconfig.base.json"
compilerOptions overrides:
  target: "ES2017"      (Next.js compatibility — overrides base ES2020)
  noEmit: true          (Next.js doesn't emit files)
  allowJs: true         (Next.js-specific)
  jsx: "react-jsx"      (React JSX transform)
  incremental: true     (Next.js incremental builds)
  plugins: [{ "name": "next" }]
  paths: { "@/*": ["./src/*"] }   (relative to apps/web/ — CRITICAL per Pitfall 2)
include: ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts", ".next/dev/types/**/*.ts", "**/*.mts"]
exclude: ["node_modules"]

<!-- components.json aliases — update paths to confirm they work with @/* in apps/web: -->
Current aliases use "@/components", "@/lib/utils" etc. — these resolve via @/* in tsconfig, so no change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move app files to apps/web/ and create app package.json + tsconfig.json</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/src/, apps/web/public/, apps/web/next.config.ts, apps/web/eslint.config.mjs, apps/web/postcss.config.mjs, apps/web/components.json</files>
  <read_first>
    - /Users/ivanmaierg/asciify/package.json (current root package.json — MUST read before the move, it contains the dep list)
    - /Users/ivanmaierg/asciify/tsconfig.json (current tsconfig — MUST read before creating apps/web/tsconfig.json)
    - /Users/ivanmaierg/asciify/components.json (current shadcn config — check aliases)
    - /Users/ivanmaierg/asciify/.planning/phases/01-monorepo-scaffolding/01-RESEARCH.md (Pattern 2, Pattern 6, Pitfall 2, Pitfall 5, Migration Execution Order Step 4)
  </read_first>
  <action>
Execute the app move. Order matters — create apps/web/ first, then move files, then run pnpm install.

**Step 1: Create apps/web/ directory and move files:**
```bash
mkdir -p /Users/ivanmaierg/asciify/apps/web

# Move app source and assets
mv /Users/ivanmaierg/asciify/src /Users/ivanmaierg/asciify/apps/web/src
mv /Users/ivanmaierg/asciify/public /Users/ivanmaierg/asciify/apps/web/public

# Move app config files
mv /Users/ivanmaierg/asciify/next.config.ts /Users/ivanmaierg/asciify/apps/web/next.config.ts
mv /Users/ivanmaierg/asciify/eslint.config.mjs /Users/ivanmaierg/asciify/apps/web/eslint.config.mjs
mv /Users/ivanmaierg/asciify/postcss.config.mjs /Users/ivanmaierg/asciify/apps/web/postcss.config.mjs
mv /Users/ivanmaierg/asciify/components.json /Users/ivanmaierg/asciify/apps/web/components.json

# Move next-env.d.ts if it exists (Next.js will regenerate if missing)
test -f /Users/ivanmaierg/asciify/next-env.d.ts && mv /Users/ivanmaierg/asciify/next-env.d.ts /Users/ivanmaierg/asciify/apps/web/next-env.d.ts || true
```

**Step 2: Create `apps/web/package.json`** (per Pattern 6):
```json
{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@base-ui/react": "^1.3.0",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "gif.js": "^0.2.0",
    "lucide-react": "^1.7.0",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "shadcn": "^4.1.2",
    "tailwind-merge": "^3.5.0",
    "tw-animate-css": "^1.4.0",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
```
Copy the exact dependency versions from the original root `package.json` — do not guess or approximate.

**Step 3: Create `apps/web/tsconfig.json`** (per D-07, Pattern 2, CRITICAL: Pitfall 2):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2017",
    "noEmit": true,
    "allowJs": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
```
CRITICAL: `"@/*": ["./src/*"]` is relative to `apps/web/` — this is what makes `@/lib/...` resolve correctly from the new location. Do NOT write `"../../src/*"` or `"./apps/web/src/*"`.

**Step 4: Delete the original root-level tsconfig.json** (it has been superseded by tsconfig.base.json + apps/web/tsconfig.json):
```bash
rm /Users/ivanmaierg/asciify/tsconfig.json
rm -f /Users/ivanmaierg/asciify/tsconfig.tsbuildinfo
```

**Step 5: Re-run pnpm install to register apps/web as a workspace member:**
```bash
cd /Users/ivanmaierg/asciify && pnpm install
```

After install, `pnpm ls -r --depth=0` should list: asciify-monorepo, @asciify/encoder@0.1.0, @asciify/player@0.1.0, web@0.1.0
  </action>
  <verify>
    <automated>test -d /Users/ivanmaierg/asciify/apps/web/src && test -f /Users/ivanmaierg/asciify/apps/web/package.json && grep -q "\"name\": \"web\"" /Users/ivanmaierg/asciify/apps/web/package.json && test -f /Users/ivanmaierg/asciify/apps/web/tsconfig.json && grep -q "../../tsconfig.base.json" /Users/ivanmaierg/asciify/apps/web/tsconfig.json && grep -q "\"@/\\*\": \\[\"./src/\\*\"\\]" /Users/ivanmaierg/asciify/apps/web/tsconfig.json && ! test -f /Users/ivanmaierg/asciify/tsconfig.json && ! test -d /Users/ivanmaierg/asciify/src && cd /Users/ivanmaierg/asciify && pnpm ls -r --depth=0 2>&1 | grep -q "web@"</automated>
  </verify>
  <done>apps/web/src/ exists with all original source files. apps/web/package.json has name "web" and all original dependencies. apps/web/tsconfig.json extends ../../tsconfig.base.json and has "@/*": ["./src/*"]. Root-level tsconfig.json and src/ are gone. pnpm ls -r --depth=0 lists web@0.1.0.</done>
</task>

<task type="auto">
  <name>Task 2: Verify app typecheck and build pass from new location</name>
  <files>apps/web/next-env.d.ts</files>
  <read_first>
    - /Users/ivanmaierg/asciify/apps/web/tsconfig.json (verify the extends and paths are correct before running typecheck)
    - /Users/ivanmaierg/asciify/apps/web/package.json (verify all deps are present)
    - /Users/ivanmaierg/asciify/.planning/phases/01-monorepo-scaffolding/01-RESEARCH.md (Pitfall 2, Pitfall 5, Verifying app after migration)
  </read_first>
  <action>
Run the verification sequence to confirm the app works from its new location.

```bash
cd /Users/ivanmaierg/asciify

# Step 1: TypeScript check — must report 0 errors
pnpm --filter web typecheck

# If typecheck fails with "@/* module not found" errors:
# Check apps/web/tsconfig.json paths: must be "@/*": ["./src/*"] (not ../../src/*)
# Check that apps/web/src/ exists and contains the app files

# If typecheck fails with "Cannot find module 'next'" or similar:
# Run: pnpm --filter web install (installs missing packages)

# Step 2: Production build — must succeed
pnpm --filter web build

# Step 3: Verify turbo pipeline (dry run — no execution)
pnpm exec turbo build --dry=json 2>&1 | head -50
# Expected: shows web, @asciify/encoder, @asciify/player in task list
```

If `next-env.d.ts` is missing from `apps/web/` (Pitfall 5), Next.js will regenerate it on first `pnpm --filter web dev` or `pnpm --filter web build`. The build step above triggers regeneration automatically.

If the build succeeds but reports any phantom dependency warnings (`Cannot find module 'X'` where X is not in apps/web/package.json), add the missing package to `apps/web/package.json` dependencies and re-run pnpm install.
  </action>
  <verify>
    <automated>cd /Users/ivanmaierg/asciify && pnpm --filter web typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>`pnpm --filter web typecheck` exits 0 with no TypeScript errors. `pnpm --filter web build` completes successfully and produces `apps/web/.next/` directory. No "Cannot find module '@/...'" errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify app runs from apps/web/</name>
  <files>apps/web/.next/</files>
  <action>Human verifies the app loads and functions correctly at localhost:3000 after running pnpm --filter web dev.</action>
  <verify>
    <automated>test -f /Users/ivanmaierg/asciify/apps/web/tsconfig.json && test -d /Users/ivanmaierg/asciify/apps/web/src && ! test -d /Users/ivanmaierg/asciify/src</automated>
  </verify>
  <done>App loads at localhost:3000 with no errors. User types "approved".</done>
  <what-built>
    The full monorepo structure is in place:
    - Root: pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .npmrc
    - apps/web/: Complete Next.js app with all source files, package.json, tsconfig.json
    - packages/encoder/ and packages/player/: Stub packages registered in workspace
    - pnpm-lock.yaml present, package-lock.json absent
    - `pnpm --filter web typecheck` passes (0 errors)
    - `pnpm --filter web build` succeeds
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/ivanmaierg/asciify && pnpm --filter web dev`
    2. Open browser to http://localhost:3000
    3. Verify the ASCII converter app loads with no visual errors or console errors
    4. Verify you can drag/drop a video or interact with the controls
    5. Press Ctrl+C to stop the dev server
    6. Confirm repo root has NO src/ directory and NO package-lock.json
  </how-to-verify>
  <resume-signal>Type "approved" if the app works correctly, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete verification (all 5 success criteria from ROADMAP.md):

```bash
cd /Users/ivanmaierg/asciify

# Criteria 1: pnpm --filter web dev starts the app
# (verified via checkpoint above)

# Criteria 2: pnpm build builds all packages in order
pnpm build
echo "Exit code: $?"

# Criteria 3: packages/ exists, package-lock.json gone, pnpm-lock.yaml exists
test -d packages && echo "packages/ OK"
test -f pnpm-lock.yaml && echo "pnpm-lock.yaml OK"
! test -f package-lock.json && echo "package-lock.json GONE OK"

# Criteria 4: tsconfig.base.json used by all packages
grep -q "../../tsconfig.base.json" apps/web/tsconfig.json && echo "web extends base OK"
grep -q "../../tsconfig.base.json" packages/encoder/tsconfig.json && echo "encoder extends base OK"
grep -q "../../tsconfig.base.json" packages/player/tsconfig.json && echo "player extends base OK"

# MONO-01: workspace layout
pnpm ls -r --depth=0
# Expected: 4 entries (asciify-monorepo root + web + encoder + player)
```
</verification>

<success_criteria>
1. `apps/web/src/app/page.tsx` exists (app source moved successfully)
2. `apps/web/tsconfig.json` extends `../../tsconfig.base.json` and has `"@/*": ["./src/*"]`
3. `apps/web/package.json` has `"name": "web"` with all original app dependencies
4. Root-level `tsconfig.json` and `src/` directory are gone
5. `pnpm --filter web typecheck` exits 0
6. `pnpm --filter web build` succeeds and creates `apps/web/.next/`
7. Human verification: app loads correctly at localhost:3000 via `pnpm --filter web dev`
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-scaffolding/01-03-SUMMARY.md`
</output>
